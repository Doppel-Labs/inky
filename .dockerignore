# Rebuilt inside the image
node_modules
dist
*.tsbuildinfo

# Local secrets / scratch — must never enter the build context
.env
.env.local
*.local
.herald-output
herald.config.json

# VCS / docs / editor noise
.git
.gitignore
docs
.playwright-mcp
.DS_Store
.idea
.vscode
*.log
